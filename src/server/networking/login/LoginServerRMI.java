package server.networking.login;

import shared.AccessType;
import shared.LoginUser;

public class LoginServerRMI implements LoginServer
{
    public LoginServerRMI()
    {

    }

    @Override
    public AccessType login(LoginUser user)
    {
        return null;
    }
}
