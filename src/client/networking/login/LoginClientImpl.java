package client.networking.login;

import shared.AccessType;
import shared.LoginUser;

public class LoginClientImpl implements LoginClient
{

  @Override public AccessType loginClient(LoginUser user)
  {
    return null;
  }
}
